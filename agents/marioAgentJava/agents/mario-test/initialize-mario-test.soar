sp {propose*initialize-mario-test
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-mario-test)
}

sp {apply*initialize-mario-test
   (state <s> ^operator <op>)
   (<op> ^name initialize-mario-test)
-->
   (<s> ^name mario-test)
}

