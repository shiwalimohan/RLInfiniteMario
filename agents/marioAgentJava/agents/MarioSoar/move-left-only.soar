sp {propose*move-left-only
   (state <s> ^name MarioSoar
              ^io.input-link.mario <m>)
   (<m> ^xi <x>
        ^yi <y>)
-->
   (<s> ^operator <o> +)
   (<o> ^name move-left-only)
}

sp {apply*move-left-only
   (state <s> ^operator <o>
              ^io <io>)
   (<io> ^output-link <out>)
   (<o> ^name move-left-only)
-->
   (<out> ^move <move>
          ^jump <j>)
   (<move> ^direction left)
   (<j> ^up no)
}
sp {apply*move-left-only*remove-move
   (state <s> ^operator.name move-left-only 
              ^io.output-link <out>)
   (<out> ^move <move>)
   (<move> ^status complete)
-->
   (<out> ^move <move> -)}
