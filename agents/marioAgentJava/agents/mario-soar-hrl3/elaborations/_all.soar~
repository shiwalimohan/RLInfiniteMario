sp {elaborate*state*name
   (state <s> ^superstate.operator.name <name>)
-->
   (<s> ^name <name>)
}

sp {elaborate*state*top-state
   (state <s> ^superstate.top-state <ts>)
-->
   (<s> ^top-state <ts>)
}
sp {elaborate*state*io
   (state <s> ^superstate.io <io>)
-->
   (<s> ^io <io>)
}
sp {elaborate*state*move
   (state <s> ^superstate.rand-move <mv>)
-->
   (<s> ^rand-move <mv>)
}
sp {elaborate*state*jump
   (state <s> ^superstate.rand-jump <jp>)
-->
   (<s> ^rand-jump <jp>)
}
sp {elaborate*state*speed
   (state <s> ^superstate.rand-speed <sp>)
-->
   (<s> ^rand-speed <sp>)
}
sp {elaborate*state*mario
   (state <s> ^superstate.mario <mario>)
-->
   (<s> ^mario <mario>)
}
sp {elaborate*state*monster
   (state <s> ^superstate.monster <m>)
-->
   (<s> ^monster <m>)
}
sp {elaborate*state*pipe
   (state <s> ^superstate.pipe <p>)
-->
   (<s> ^pipe <p>)
}
sp {elaborate*state*pit
   (state <s> ^superstate.pit <p>)
-->
   (<s> ^pit <p>)
}
sp {elaborate*state*platform
   (state <s> ^superstate.platform <p>)
-->
   (<s> ^platform <p>)
}
