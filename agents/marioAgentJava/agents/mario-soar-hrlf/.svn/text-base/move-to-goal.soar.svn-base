sp {propose*move-to-goal1
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
            -^monster.isthreat yes 
            - ^coin.isreachable yes
            - ^question.isreachable yes)
-->
   (<s> ^operator <o> + <)
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal2
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
            ^monster.isthreat yes 
            - ^coin.isreachable yes
            - ^question.isreachable yes)
-->
   (<s> ^operator <o> + <)
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal3
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
           - ^monster.isthreat yes 
             ^coin.isreachable yes
            - ^question.isreachable yes)
-->
   (<s> ^operator <o> + < )
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal4
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
           - ^monster.isthreat yes 
           -  ^coin.isreachable yes
            ^question.isreachable yes)
-->
   (<s> ^operator <o> + < )
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal5
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
            ^monster.isthreat yes 
            - ^coin.isreachable yes
             ^question.isreachable yes)
-->
   (<s> ^operator <o> + < )
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal6
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
            ^monster.isthreat yes 
             ^coin.isreachable yes
            - ^question.isreachable yes)
-->
   (<s> ^operator <o> + < )
   (<o> ^name move-to-goal)
}
sp {propose*move-to-goal7
   (state <s> ^name mario-soar-hrlf
             ^mario <mario>
           - ^monster.isthreat yes 
             ^coin.isreachable yes
             ^question.isreachable yes)
-->
   (<s> ^operator <o> + < )
   (<o> ^name move-to-goal)
}


sp {move-to-goal*propose*action1
   (state <s> ^name move-to-goal
              ^tile-type <tt>)
   (<tt> ^right |7|)
-->
   (<s> ^operator <op> +)
   (<op> ^name action
         ^direction right
         ^jump yes
         ^speed low)
}
sp {move-to-goal*propose*action2
   (state <s> ^name move-to-goal
              ^tile-type <tt>)
   (<tt> ^right |\||)
-->
   (<s> ^operator <op> +)
   (<op> ^name action
         ^direction right
         ^jump yes
         ^speed low)
}
sp {move-to-goal*propose*action3
   (state <s> ^name move-to-goal
              ^tile-type <tt>)
  - (<tt> ^right <<|7| |\||>>)
-->
   (<s> ^operator <op> +)
   (<op> ^name action
         ^direction right
         ^jump no
         ^speed low)
}