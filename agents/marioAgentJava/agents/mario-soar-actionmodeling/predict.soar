#-- if there are no objects in the state then no collision can occur, and all operators are assigned a 0 numeric value

#sp {evaluate-operator*apply*selected*operator
#   (state <s> ^name evaluate-operator
#   	      ^tried-tied-operator
#   	      -^object
#	      ^desired <d>)
#-->
#   (<s> ^failure <d>)
#}