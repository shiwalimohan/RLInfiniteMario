sp {propose*move-left-jump
   (state <s> ^name MarioSoar
              ^io.input-link.mario <m>)
   (<m> ^xi <x>
        ^yi <y>)
-->
   (<s> ^operator <o> +)
   (<o> ^name move-left-jump)
}

sp {apply*move-left-jump
   (state <s> ^operator <o>
              ^io <io>)
   (<io> ^output-link <out>)
   (<o> ^name move-left-jump)
-->
   (<out> ^move <move>
          ^jump <j>)
   (<move> ^direction left)
   (<j> ^up yes)
}
sp {apply*move-left-jump*remove-move
   (state <s> ^operator.name move-left-jump
              ^io.output-link <out>)
   (<out> ^move <move>)
   (<move> ^status complete)
-->
   (<out> ^move <move> -)}
